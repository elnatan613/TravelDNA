# Runtime dependencies for the FastAPI + React service.
# Streamlit, pytest and the data-collection-only translator stay in requirements.txt
# for local development, but should not be part of the production image.
requests
google-genai
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
