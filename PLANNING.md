# TravelDNA - תוכנית עבודה

## מבנה התיקיות ותוכן כל תיקייה

```
travel-dna/
├── config.py
├── data/
│   ├── raw/
│   └── processed/
├── scripts/
│   ├── destination_scraper.py
│   ├── numbeo_fetcher.py
│   └── kashrut_fetcher.py
├── nlp/
│   └── profile_extractor.py
├── matching/
│   └── matcher.py
├── app/
│   └── demo.py
└── tests/
```

**`config.py`** (בשורש, לא בתוך תיקייה)
כל ההגדרות המשותפות: שמות הצירים, רשימת הערים, רמות הכשרות. זה "מילון המונחים"
המשותף - שני הצדדים קוראים ממנו ולא מגדירים שום דבר מהערכים האלה בעצמם.

**`data/raw/`**
תוצרי ביניים גולמיים - קבצי JSON/CSV גולמיים כפי שהתקבלו מ-API-ים לפני עיבוד
(לדוגמה: תגובת GeoNames המקורית, רשימת POIs גולמית מ-OpenTripMap). שימושי
לדיבאג ולבדוק שלא איבדתם מידע בדרך.

**`data/processed/`**
הפלט הסופי המוכן לשימוש - בעיקר `destinations.json`, קובץ אחד עם כל הערים
וכל הציונים שלהן על כל הצירים. זה הקובץ היחיד שחלק ה-matching צריך לקרוא.

**`scripts/`**
כל סקריפט אחראי על ציר אחד או קבוצת צירים, ומזין את `data/processed/`:
- `destination_scraper.py` - עירוני/טבע, תרבות/בילוי (GeoNames + OpenTripMap)
- `numbeo_fetcher.py` - תקציב/יוקרה (Numbeo)
- `kashrut_fetcher.py` - קהילתי/יהודי (חב"ד, מסעדות כשרות, ביקורות)

**`nlp/`**
כל מה שקשור לצד המשתמש: לוקח טקסט חופשי ומחזיר וקטור פרופיל. גם הלוגיקה
להמרת ערכי הסליידרים למשקלים נמצאת כאן, כי היא גם "צד המשתמש".

**`matching/`**
הלוגיקה שמשווה בין וקטור מטייל (מ-`nlp/`) לוקטורי יעדים (מ-`data/processed/`)
ומחזירה דירוג. לא יודע ולא צריך לדעת איך הווקטורים הופקו - רק עובד איתם.

**`app/`**
הממשק שמחבר הכל יחד (כרגע Streamlit) - קורא ל-`nlp/`, קורא ל-`matching/`, מציג תוצאה.

**`tests/`**
בדיקות - חשוב בעיקר ל-`matching/` (כדי לוודא שהנוסחה עובדת נכון) ול-`nlp/`
(לבדוק שהמודל לא "משתגע" על קלט קיצוני).

---

## חלוקת העבודה

**אלנתן - "צד היעדים"**
`scripts/` + `matching/` + `data/`
כולל: שליפת דאטה משלושת המקורות, בניית `destinations.json` השלם, מימוש
נוסחת המרחק המשוקלל, בדיקות תקינות (`tests/`).

**דין - "צד המשתמש"**
`nlp/` + חלק מ-`app/`
כולל: עיצוב השאלות הפתוחות, איסוף/יצירת דאטהסט לאימון (תשובות מתויגות),
אימון המודל שממיר טקסט לוקטור, מימוש ההמרה של הסליידרים למשקלים.

**משותף**
`config.py` (רק בתיאום), `app/demo.py` (חיבור סופי, אחרי ששני הצדדים עובדים),
מצגת סיום.

הסיבה לחלוקה הזו: שני הצדדים לא נוגעים באותם קבצים, ושניכם יכולים לעבוד
מול ערכי דמה של הצד השני (`extract_profile_dummy` / `destinations.json` לדוגמה)
בלי לחכות אחד לשני אף רגע.

---

## שלבי העבודה (זרימה הגיונית)

**שלב 1 - יסודות (שבוע 1)**
לפני שמתחילים לבנות בפועל, לוודא שהחוזה המשותף סגור:
- לסגור סופית את רשימת הצירים ב-`config.py`
- לסגור את רשימת הערים ל-MVP (10 הערים שכבר בחרנו)
- כל אחד מריץ את השלד הקיים אצלו ומוודא שהוא רץ

*למה קודם כל זה: כל עיכוב כאן עוצר את שני הצדדים, כי שניהם תלויים ב-`config.py`.*

**שלב 2 - עבודה מקבילה (שבועות 2-4)**

*אלנתן:*
1. להשיג מפתחות API (GeoNames, OpenTripMap) ולהריץ בפועל את `destination_scraper.py`
2. לבדוק שהתוצאות הגיוניות (לבדוק ידנית 2-3 ערים מוכרות)
3. לממש את `numbeo_fetcher.py` (תקציב)
4. לממש את `kashrut_fetcher.py` (קהילתיות) - הכי מורכב, לכן אחרון

*דין:*
1. לנסח את השאלות הפתוחות הסופיות (2-3 שאלות)
2. לאסוף דאטהסט ראשוני - לבקש מכמה חברים/משפחה לענות על השאלות, לתייג ידנית
3. לבנות ולאמן גרסה ראשונה של מודל חילוץ הפרופיל
4. לממש את לוגיקת הסליידרים למשקלים (`extract_importance_weights` - כבר קיים שלד)

*למה זה מקביל: `matcher.py` כבר עובד עם דאטה דמה, אז אף אחד לא "תקוע" מחכה לשני.*

**שלב 3 - חיבור ראשוני (שבוע 5)**
מחברים את שני הצדדים לראשונה בתוך `app/demo.py` - גם אם עדיין לא הכל מושלם
(למשל דאטה על 10 ערים בלבד, מודל NLP בגרסה ראשונית). המטרה: לראות שהכל
"מדבר" ביחד לפני שממשיכים לשפר.

*למה עכשיו ולא בסוף: אם יש בעיית אינטגרציה (פורמט לא תואם, וכו') עדיף לגלות
עכשיו ולא שבוע לפני ההגשה.*

**שלב 4 - שיפור ובדיקות (שבועות 6-7)**
- הרחבת רשימת הערים אם יש זמן
- שיפור דיוק מודל ה-NLP (יותר דוגמאות אימון, כוונון)
- בדיקות (`tests/`) - במיוחד למקרי קצה (למשל: מטייל שלא ענה על שאלה, יעד עם דאטה חסר)
- לתעד ממצאים מעניינים מה-EDA לצורך המצגת

**שלב 5 - מצגת (שבוע 8)**
בניית סיפור ההצגה: מוטיבציה → איך אוספים דאטה → איך בונים פרופיל → איך
מתאימים → דוגמאות תוצאה חיות. לזכור: אסור להראות קוד במצגת, רק רעיונות
והחלטות עיצוב.

---

## נקודות סנכרון קריטיות בין השלבים

- **אחרי שלב 1**: לוודא ששניכם עובדים על אותה גרסת `config.py` (לא לשנות בלי תיאום)
- **לפני שלב 3**: לוודא שפורמט `destinations.json` שאלנתן מייצר תואם בדיוק למה שדין
  מצפה לקבל ב-`matcher.py` (השדות `city`, `axes`, וכו')
- **אחרי שלב 3**: ישיבת סטטוס קצרה - מה עובד, מה לא, מה עדיפות לשבועות הנותרים
