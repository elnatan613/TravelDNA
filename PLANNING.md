# TravelDNA — מצב הפרויקט וכללי עבודה

## מטרת הפרויקט

TravelDNA ממליץ על יעד אירופי לפי פרופיל מטייל: שאלון, טקסט חופשי ו־7 צירי
העדפה. מנוע ההתאמה מחזיר יעדים מדורגים; עבור חלק מהערים הסוכן יכול גם לבנות
מסלול יום־יום מבוסס מקורות תיירותיים.

## מבנה וחוזה משותף

```
TravelDNA/
├── config.py                 # החוזה המשותף: צירים, ערים וסף כשרות
├── scripts/                  # איסוף ועיבוד נתוני יעדים
├── data/processed/           # destinations.json — נתוני היעדים המוכנים
├── matching/                 # דירוג יעדים וסינון כשרות
├── nlp/                      # חילוץ פרופיל מטקסט ושאלון
├── rag/                      # מאגר הידע התיירותי וה־retriever
├── agent/                    # סוכן לבניית מסלול
├── app/                      # דמו Streamlit
└── tests/                    # בדיקות אוטומטיות
```

`config.py` הוא מקור האמת. שבעת הצירים הרשמיים הם:

| ציר | 0 | 1 |
| --- | --- | --- |
| `urban` | טבע/כפר | עיר |
| `culture` | עניין נמוך | עניין גבוה |
| `nightlife` | מעט בילוי | הרבה בילוי |
| `social` | שקט/עצמאי | חברתי |
| `activity_density` | קצב רגוע | ימים עמוסים |
| `food` | לא מרכזי | חשוב מאוד |
| `price_sensitivity` | פתוח ליוקרה | רגיש למחיר |

כשרות אינה ציר: זהו אילוץ קשיח. כאשר המשתמש מסמן דרישת כשרות,
`matching/matcher.py` מסנן יעדים עם `kosher_availability` נמוך לפני הדירוג.

## מצב נוכחי

- `destinations.json` מכיל 18 ערים אירופיות ווקטור מלא על כל 7 הצירים.
- הדמו ב־`app/demo.py` מחבר שאלון, Gemini לטקסט חופשי, התאמה וסינון כשרות.
- ה־RAG קיים עבור Paris, Prague ו־Vienna. שאר 15 הערים עדיין אינן זמינות
  לבניית מסלול מפורט.
- `agent/trip_planner.py` בונה מסלול לערי ה־RAG באמצעות Gemini.
- בעת העדכון האחרון כל 53 הבדיקות עברו.

## מקורות נתונים

| שימוש | מקור |
| --- | --- |
| קואורדינטות ואוכלוסייה | GeoNames |
| נקודות עניין, תרבות, בילוי, אוכל וכשרות | OpenStreetMap באמצעות Overpass API |
| מחירים | Numbeo — טבלה ידנית של מדד יוקר מחיה |
| ידע למסלולים | Wikivoyage |
| חילוץ טקסט וסוכן | Gemini API |

`data/processed/destinations.json` נכנס ל־Git בכוונה: כך כולם יכולים להריץ
את המוצר בלי מפתחות API לאיסוף הנתונים.

## חלוקת עבודה

- אלנתן: `scripts/`, `matching/`, `data/`, RAG והסוכן.
- דין: `nlp/`. אין לערוך שם ללא בקשה מפורשת של אלנתן או תיאום עם דין.
- `config.py` ו־`app/demo.py` משותפים; שינויים בהם חייבים לשמור על החוזה
  המשותף.

## כללי Git ועבודה

- לפני שינוי בודקים `git status` ולא דורסים שינויים של אחרים.
- אחרי שינוי פונקציונלי מריצים בדיקות רלוונטיות ויוצרים commit ברור.
- לא מבצעים `git push` בלי אישור מפורש של אלנתן.
- אין לבצע `reset --hard`, מחיקות רחבות או דריסת עבודה קיימת בלי אישור מפורש.
- `.env`, מפתחות API וסודות אחרים לעולם לא נכנסים ל־Git או לפלט.
- בסיום עבודה מדווחים בקצרה מה השתנה, אילו בדיקות רצו ומה נשאר פתוח.

## הרצה ובדיקות

ה־Python הזמין במחשב:

```bat
C:\Users\elnat\anaconda3\python.exe
```

הרצת כל הבדיקות:

```bat
C:\Users\elnat\anaconda3\python.exe -m pytest tests/ -q
```

הרצת הדמו בלחיצה כפולה:

```text
run_demo.bat
```

או דרך טרמינל:

```bat
C:\Users\elnat\anaconda3\python.exe -m streamlit run app\demo.py
```

## המשימה הבאה המוצעת

להרחיב את מאגר ה־RAG מ־3 ל־18 ערים, להריץ בדיקות חיפוש ידניות, ואז לחבר
אפשרות לקבלת מסלול מהדמו עבור עיר שנבחרה.
